meta {
  name: Login Failed
  type: http
  seq: 3
}

post {
  url: {{BASE_URL}}/admin/login
  body: json
  auth: none
}

body:json {
  {
    "email": "admin@gmail.com",
    "password": "admins"
  }
}

assert {
  res.body.status: isBoolean
  res.body.message: isDefined
}

tests {
  test("status code is 401", function(){
    expect(res.getStatus()).to.equal(401)
  })
  
  test("message is Email atau password anda salah!", function(){
    expect(res.getBody().message).to.equal("Email atau password anda salah!")
  })
}
